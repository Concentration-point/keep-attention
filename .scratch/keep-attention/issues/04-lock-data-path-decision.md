## Question

综合票 02（Orca 数据获取探针）与票 03（DeepSeek API 核查）的实测结果，**落锤本效应的终点决策**：

- **能不能做**：焦点信号 + 各终端上下文是否都能以可接受代价拿到？
- **走哪条数据路径**：焦点来源（CLI 字段 / 旁路 / 拿不到时的降级）、上下文来源（terminal read 的完整度）、总结后端（DeepSeek 接入方式）。
- **边界与降级**：哪些数据拿不到、对应的降级呈现是什么。

这是一次 grilling（HITL）——与用户一起把决策拍死。产出：一段明确的“数据获取路径决策”，写入 map 的 Decisions so far，并把仍在雾区的 UI/脱敏/频率等 graduate 成新票（或留待下一效应）。

Blocked by: 02, 03
