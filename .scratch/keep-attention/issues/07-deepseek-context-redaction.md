## Question

发给 DeepSeek 的终端上下文如何脱敏/裁剪？（合规硬约束）

- 背景：DeepSeek 服务器在中国境内，终端上下文可能含字节内部代码/日志，外发属数据出境高风险（#4 记录）。
- 发什么、不发什么：只发高度概括的任务元信息，还是发原始渲染文本？如何在发送前做本地裁剪（截断、去敏感串、只取最近 N 行）？
- 是否需要用户可开关/白名单机制来控制哪些 worktree 允许外发？

依赖：#5。产出：上下文外发的脱敏/裁剪规则，供 spec 采纳。
