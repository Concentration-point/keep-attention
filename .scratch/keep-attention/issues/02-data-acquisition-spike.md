## Question

用 `orca-cli` 真的跑通一条最小数据获取链路，证明数据能拿到、拿到的是什么形态。至少验证：

1. **列出终端**：实际运行 CLI 列出当前 Orca 终端/agent，粘贴真实输出样例（脱敏）。
2. **读取内容**：对其中一个正在跑 agent 的终端，实际读取其内容，记录“能读到多完整”“格式如何”。
3. **焦点判定**：验证票 01 查到的焦点信号是否真能用；若 CLI 不直接给焦点，实测一条可行的旁路（或明确记录“拿不到”）。

产出：真实命令 + 脱敏输出样例 + 一句结论“数据获取可行/部分可行/不可行，走哪条路径”。这是票 04 落锤最硬的证据。

Blocked by: 01
